# FireCommandCenter

def add(request):
	contains = ''
	hasKeyLocated = ''
	hasLocation = ''

	fireCommandCenter = FireCommandCenter(contains = contains,hasKeyLocated = hasKeyLocated,hasLocation = hasLocation)
	fireCommandCenter.save()